//Global Variables

void setup() {
  size(1000,700);
  println("moniter Dimensions: width ", displayWidth, "height", displayHeight);
  if (width > displayWidth || height*0.99 > displayHeight) { //Will Canvas dislplay on Moniter Geometry
    println("Canvas will not fit in the moniter. /nChange the size() dimentionsr");
    println("Program is closing automatically.");
    exit();
  }
  if() {
  }
}

void draw() {
}

void mouseClicked() {
}
